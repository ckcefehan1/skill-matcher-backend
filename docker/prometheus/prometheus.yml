global:
  scrape_interval: 15s

scrape_configs:
  - job_name: skill-matcher-backend
    metrics_path: /actuator/prometheus
    # Backend runs on host via bootRun, not in compose
    static_configs:
      - targets:
          - host.docker.internal:8080
